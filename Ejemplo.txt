Configuracion inicial

git init
git add . //Agrega todos los archivos
git add "archivo.extension"
git config --global user.email "ivanbn.isc@gmail.com"
git config --global user.name "IvanBN"
git pull origin master //Para traer todas las modificaciones realizadas
git commit -m "first commit"
git remote remove //Eliminar algo
git remote add origin https://github.com/IvanBN/php_drupal.git
git push -u origin master
git status //Permite ver el estatus de los arhivos
git romete //Permite ver el origen
